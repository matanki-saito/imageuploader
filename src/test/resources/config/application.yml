spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:63306/testdb?characterEncoding=UTF-8&serverTimezone=JST
    username: root
    password: 1qaz2wsx
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
    initialization-mode: always
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 10MB
  redis:
    host: localhost
    port: 6379
